# TEAM_424: Terminal feature configuration
# Home Manager settings specific to the terminal feature
{ config, pkgs, lib, ... }:

{
  # Terminal-specific Home Manager configuration goes here
  # Currently empty as all program configs are in modules/programs/
  # This file is for feature-level settings that don't belong to a single program
}
